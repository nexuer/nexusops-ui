import type { PluginOption } from "vite";

export function configurePlugins(rootDir: string, env: Partial<ImportMetaEnv>, isProductionBuild: boolean): PluginOption[] {
  const vitePlugins: PluginOption[] = [

  ];
  return vitePlugins;
}
